apiVersion: v1
kind: Service
metadata:
  name: # TODO: name it "web-nodeport"
spec:
  type: # TODO: NodePort
  selector:
    app: # TODO: must match Deployment pod labels
  ports:
    - port: 80
      targetPort: 80
      nodePort: # TODO: set to 30080

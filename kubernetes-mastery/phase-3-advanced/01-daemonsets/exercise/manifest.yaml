# ============================================================================
# Exercise 3.1 — DaemonSets
# ============================================================================

# TODO: Set the correct apiVersion for DaemonSet (hint: apps/v1)
apiVersion: ???
kind: DaemonSet

metadata:
  name: log-collector
  labels:
    app: log-collector

spec:
  selector:
    matchLabels:
      # TODO: Set the selector label
      app: ???

  updateStrategy:
    type: RollingUpdate

  template:
    metadata:
      labels:
        # TODO: Add label app=log-collector (must match selector)
        app: ???
    spec:
      # TODO: Add a toleration so this DaemonSet can run on control-plane nodes
      # tolerations:
      #   - key: node-role.kubernetes.io/control-plane
      #     operator: Exists
      #     effect: NoSchedule

      containers:
        # TODO: Define a container named "collector" using "busybox" image
        # Command: sh -c "while true; do echo $(date) collecting logs; sleep 10; done"
        - name: ???
          image: ???
          command: ["sh", "-c", "while true; do echo $(date) collecting logs; sleep 10; done"]
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "64Mi"
              cpu: "100m"
          # TODO: Mount the host log directory (read-only)
          # volumeMounts:
          #   - name: varlog
          #     mountPath: /var/log/host
          #     readOnly: true

      volumes:
        # TODO: Add a hostPath volume pointing to /var/log
        # - name: varlog
        #   hostPath:
        #     path: /var/log

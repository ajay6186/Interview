# ============================================================================
# Exercise 2.5 — Health Probes
# ============================================================================

apiVersion: v1
kind: Pod
metadata:
  name: probe-pod
spec:
  containers:
    - name: app
      image: nginx:alpine
      ports:
        - containerPort: 80

      # TODO: Add a livenessProbe using HTTP GET /healthz on port 80
      # Restart the container if it fails 3 times
      # livenessProbe:
      #   httpGet:
      #     path: /healthz
      #     port: 80
      #   initialDelaySeconds: 10   # Wait 10s before first probe
      #   periodSeconds: 10         # Check every 10s
      #   failureThreshold: 3       # Restart after 3 failures

      # TODO: Add a readinessProbe using HTTP GET / on port 80
      # Remove from Service endpoints if it fails
      # readinessProbe:
      #   httpGet:
      #     path: /
      #     port: 80
      #   initialDelaySeconds: 5
      #   periodSeconds: 5
      #   failureThreshold: 3

      # TODO: Add a startupProbe for slow-starting apps
      # This gives up to 30 probes * 10s = 300s for the app to start
      # startupProbe:
      #   httpGet:
      #     path: /
      #     port: 80
      #   failureThreshold: 30
      #   periodSeconds: 10

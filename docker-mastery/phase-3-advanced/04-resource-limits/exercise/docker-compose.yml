# ============================================================================
# Exercise 3.4 — Resource Limits
# ============================================================================

version: "3.9"

services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    # TODO: Add resource limits (256MB RAM, 0.5 CPU)
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '0.50'
    #       memory: 256M
    #     reservations:
    #       memory: 128M

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_PASSWORD: secret
    # TODO: Add resource limits (512MB RAM, 1 CPU)
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '1.00'
    #       memory: 512M

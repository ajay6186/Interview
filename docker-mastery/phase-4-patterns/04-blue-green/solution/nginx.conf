events {}

http {
    upstream app {
        server blue:3000;    # Switch to "server green:3000;" for green deployment
    }

    server {
        listen 80;
        location / {
            proxy_pass http://app;
            proxy_set_header Host $host;
        }
    }
}

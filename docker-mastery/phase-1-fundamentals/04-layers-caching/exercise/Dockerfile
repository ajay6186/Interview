# ============================================================================
# Exercise 1.4 — Layers and Build Cache
# Order your COPY and RUN instructions for maximum cache reuse.
# ============================================================================

FROM node:18-alpine

WORKDIR /app

# TODO: Copy ONLY package.json (and package-lock.json if present) first
# This allows Docker to cache the npm install layer
# COPY ...

# TODO: Run npm install
# RUN ...

# TODO: NOW copy the rest of the source code
# COPY ...

EXPOSE 3000
CMD ["node", "index.js"]

# ============================================================================
# Exercise 2.5 — Health Checks in docker-compose
# ============================================================================

version: "3.9"

services:
  web:
    build: .
    ports:
      - "3000:3000"
    # TODO: Add healthcheck:
    # healthcheck:
    #   test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/health"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
    #   start_period: 5s

  api:
    image: node:18-alpine
    command: sh -c "echo 'API started' && while true; do sleep 1; done"
    depends_on:
      web:
        # TODO: Change from service_started to service_healthy
        condition: service_started

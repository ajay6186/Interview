# ============================================================================
# Exercise 2.4 — Docker Networks
# ============================================================================

version: "3.9"

services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    # TODO: Attach web to the "frontend" network only
    # networks:
    #   - frontend

  api:
    image: node:18-alpine
    command: sh -c "while true; do sleep 1; done"
    # TODO: Attach api to BOTH frontend and backend networks
    # networks:
    #   - frontend
    #   - backend

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_PASSWORD: secret
    # TODO: Attach db to the "backend" network only
    # networks:
    #   - backend

# TODO: Declare both networks
# networks:
#   frontend:
#   backend:

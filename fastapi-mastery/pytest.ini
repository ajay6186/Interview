[pytest]
# Show short summary for all failures, errors, and passed tests
addopts = -v --tb=short

# Test discovery
testpaths = tests

# Custom markers — use with:  @pytest.mark.smoke
markers =
    smoke: quick sanity checks (run with -m smoke)
    security: security boundary tests (run with -m security)
    integration: tests that exercise multiple layers together

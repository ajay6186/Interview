# ============================================================================
# Exercise 2.5 — Chart Dependencies
# ============================================================================

apiVersion: v2
name: with-dependencies
description: Exercise 2.5 - Chart Dependencies
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  # TODO: Add a dependency on bitnami/redis
  # - name: redis
  #   version: ">=17.0.0"
  #   repository: "https://charts.bitnami.com/bitnami"
  #   condition: redis.enabled   # Enabled/disabled by values.yaml
  - name: ???
    version: ???
    repository: ???
    condition: ???

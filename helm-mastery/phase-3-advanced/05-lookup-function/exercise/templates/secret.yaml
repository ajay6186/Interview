{{- $secretName := printf "%s-app-secret" .Release.Name }}
{{- $namespace := .Release.Namespace }}

{{- /* TODO: Use lookup to check if the secret already exists */}}
{{- /* Signature: lookup "apiVersion" "Kind" "namespace" "name" */}}
{{- $existing := lookup "???" "???" $namespace $secretName }}

{{- /* TODO: Only create the secret if it doesn't exist yet */}}
{{- if not ??? }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
type: Opaque
stringData:
  # TODO: Generate a random 32-char alphanumeric password
  password: {{ randAlphaNum ??? | quote }}
  created-at: {{ now | date "2006-01-02" | quote }}
{{- end }}

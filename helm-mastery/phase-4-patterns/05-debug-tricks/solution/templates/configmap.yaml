apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
data:
  # FIX 4: toYaml must be piped through nindent to preserve correct indentation
  config: |
    {{- .Values.config | toYaml | nindent 4 }}

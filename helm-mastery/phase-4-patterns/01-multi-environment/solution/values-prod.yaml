replicaCount: 5

image:
  tag: "1.0.0"
  pullPolicy: IfNotPresent

resources:
  requests:
    cpu: "500m"
    memory: "512Mi"
  limits:
    cpu: "2000m"
    memory: "1Gi"

ingress:
  enabled: true
  host: myapp.example.com

config:
  logLevel: "warn"
  debug: false
